/**
 * @file argumentHandler.js
 * CLI argument handler that parses user input and executes the appropriate command.
 *
 * Responsibilities:
 * - Parse command-line arguments.
 * - Match arguments with available command implementations.
 * - Invoke the corresponding command function.
 *
 * @example
 * // Run the "build" command
 * node index.js build
 *
 * @example
 * // Show available commands
 * node index.js help
 */
